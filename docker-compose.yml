services:
  app:
    build: .
    container_name: wordform-report-export
    ports:
      - "8000:8000"
    volumes:
      - ./reports:/app/reports
      - ./uploads:/app/uploads
    restart: unless-stopped